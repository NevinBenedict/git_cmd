git clone <url> => create the repository to our local workspace

git status => to check whether git has initialised in ur current directory

git add <file name> => add the file "filename" to staging area 

git add . => add all the files in the directory to the staging area

git commit -m "message" =>to commit that it has tracked by the git

git push -u origin main => to push the commited file to repository

git init => to initialise git in a folder